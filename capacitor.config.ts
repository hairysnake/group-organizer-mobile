import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'group-organizer-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
